# Session

## How to install

1. Add the package ```philipschlender/session-php``` to the ```require``` or ```require-dev``` element of the ```composer.json``` file

    ```bash
    composer require philipschlender/session-php
    ```
